import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  modalTitle: {
    marginTop: 12,
    width: '80%',
    alignSelf: 'center',
  },
  cancelBtn: {
    borderWidth: 2,
    borderRadius: 12,
  },
});

export default styles;
