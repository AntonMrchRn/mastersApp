import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  banner: {
    marginTop: 15,
  },
  preview: {
    flexDirection: 'row',
  },
  defaultText: {
    flexShrink: 1,
    marginLeft: 8,
  },
});

export default styles;
