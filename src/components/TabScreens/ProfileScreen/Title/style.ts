import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  btn: {
    flex: 1,
    paddingHorizontal: 0,
    paddingVertical: 0,
  },
  label: {
    marginLeft: 8,
    fontSize: 15,
    lineHeight: 20,
  },
});

export default styles;
