import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  defaultText: {
    flexShrink: 1,
    marginLeft: 8,
  },
  contractors: {
    flexDirection: 'row',
  },
});

export default styles;
