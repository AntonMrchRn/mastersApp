import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapperIconHistory: {
    width: 48,
    height: 48,
    justifyContent: 'center',
    alignItems: 'center',
  },
  txt: {
    textAlign: 'left',
  },
  wrapperLoading: {
    width: '100%',
    height: '70%',
    justifyContent: 'center',
    alignItems: 'center',
  },
  otes: {
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'row',
    marginTop: 24,
  },
  desc: {
    flexShrink: 1,
    marginLeft: 8,
  },
});
