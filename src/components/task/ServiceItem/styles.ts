import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  ml4: {
    marginLeft: 4,
  },
  rowDirection: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 4,
  },
  itemTitle: {
    marginVertical: 8,
  },
  items: {
    gap: 4,
  },
});
