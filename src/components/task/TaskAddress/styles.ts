import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  address: {
    flexDirection: 'row',
    marginBottom: 5,
  },
  ml10: {
    marginLeft: 5,
  },
  wrapperIcon: {
    width: 20,
    alignItems: 'center',
    bottom: 1.2,
  },
  id: {
    marginTop: 4,
  },
});
