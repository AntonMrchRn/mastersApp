import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#ffefe6',
    width: '100%',
    padding: 7,
    borderRadius: 5,
  },
  text: {
    color: '#636363',
    fontSize: 12,
  },
});
